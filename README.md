# Comandos-simples-de-python-1
São questões simples para minha pessoa ter uma ser noção do meu progresso. 

id = int(input('Digite seu ID: '))
nome = input('Digite seu nome: ')
salario = float(input('Digite seu salário: '))

brasileiro = input('Você é brasileiro? (sim/não): ')
btasileiro = brasileiro.lower() == 'sim'

print(f'\nID: {id} \nNome: {nome} \nSalário: R${salario} \nBrasilero: {brasileiro}')

# Se alguém quiser complementar o comando acima poderia me enviar a o complemento...
